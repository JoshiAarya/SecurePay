{
    "build": {
      "env": {
        "DATABASE_URL": "@database_url"
      }
    },
    "installCommand": "npm install --workspaces && npx prisma generate",
    "buildCommand": "npx prisma generate && next build"
  }
  